# P0修复测试指南

## 如何测试修复

### 🎵 测试 #1: BGM循环修复

**步骤:**
1. 在浏览器中打开 `index.html`
2. 开启音效（点击右上角音效按钮）
3. 观察BGM是否持续播放

**预期结果:**
- ✅ BGM应该循环播放，不会在2个音符后停止
- ✅ 音乐应该自然过渡，没有中断

**如何验证是否失败:**
- ❌ BGM只播放2个音符就完全停止

---

### ⚖️ 测试 #2: EV计算上限修复

**步骤:**
1. 开始新游戏或加载存档
2. 选择一只宝可梦进行多次战斗
3. 持续击败野生宝可梦直到接近510 EV
4. 打开浏览器开发者工具控制台
5. 输入: `console.log(G.player.pokemon[0].evs)`
6. 计算总EV: HP + ATK + DEF + SPA + SPD + SPE

**预期结果:**
- ✅ EV总和不应超过510
- ✅ 接近510时，进一步战斗不再增加EV

**如何验证是否失败:**
- ❌ EV总和超过510（例如：512, 514等）

---

### 🧹 测试 #3: 战斗计时器清理

**步骤:**
1. 打开浏览器开发者工具
2. 进入 Console 标签
3. 进行5场连续战斗（野生或训练师）
4. 在每场战斗结束后观察控制台是否有错误
5. 检查内存使用（Performance Monitor）

**预期结果:**
- ✅ 战斗结束后没有JavaScript错误
- ✅ 控制台没有 "Cannot read property of null" 错误
- ✅ 内存使用稳定，不持续增长
- ✅ 战斗结束后没有延迟动画或回调触发

**如何验证是否失败:**
- ❌ 控制台出现 `TypeError: Cannot read property 'hp' of null`
- ❌ 战斗结束后仍有动画触发
- ❌ 内存持续增长

---

## 快速验证脚本

在浏览器控制台运行以下脚本进行快速检查：

### 检查EV总和
```javascript
// 检查队伍中第一只宝可梦的EV
const pkm = G.player.pokemon[0]
const totalEV = Object.values(pkm.evs).reduce((a,b) => a+b, 0)
console.log(`宝可梦: ${pkm.name}`)
console.log(`EV分布:`, pkm.evs)
console.log(`EV总和: ${totalEV}`)
console.log(`是否合法: ${totalEV <= 510 ? '✅ 是' : '❌ 否，超过上限！'}`)
```

### 检查战斗计时器
```javascript
// 检查是否有活跃的战斗计时器数组
console.log(`战斗计时器数组长度: ${battleTimers.length}`)
console.log(`当前战斗状态: ${G.battle ? '进行中' : '无'}`)
```

---

## 问题排查

### 如果游戏界面不显示

**症状:** 只看到标题和"加载中"，中间游戏界面消失

**可能原因:**
1. JavaScript语法错误

**排查步骤:**
1. 打开浏览器开发者工具 (F12)
2. 查看 Console 标签
3. 查找红色错误信息

**常见错误:**
- `SyntaxError: missing ) after argument list` - 括号不匹配
- `ReferenceError: xxx is not defined` - 变量未定义

**解决方法:**
- 检查最近修改的文件
- 确认所有括号、花括号、方括号都正确闭合
- 运行 `node -c js/battle.js` 验证语法

---

### 如果BGM仍然不工作

**可能原因:**
1. 浏览器自动播放策略阻止
2. 音效未开启

**解决方法:**
1. 手动点击音效按钮
2. 与页面进行交互后再测试
3. 检查浏览器控制台是否有音频相关警告

---

## 成功标志

所有3个P0修复成功的标志：
- ✅ 游戏正常加载，界面完整显示
- ✅ BGM循环播放不中断
- ✅ 进行10场战斗后无错误或内存泄漏
- ✅ EV总和始终 ≤ 510

---

## 反馈

如果测试发现问题：
1. 记录控制台错误信息
2. 记录复现步骤
3. 检查 `P0_BUG_FIXES.md` 了解修复细节
